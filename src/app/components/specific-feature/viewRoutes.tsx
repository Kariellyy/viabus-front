import React from "react"; 

export default function ViewRoutes({ activePage }: ViewRoutesProps) {
  return (
    <>
      {activePage === "visualizarRotas" && (
        <div className="card">
          <h1>oi</h1>
        </div>
        
        )}
    </>

  )
}
